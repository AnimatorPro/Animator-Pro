main()
{
// This brings up a file requestor to load and resave
// a .bmp file.
char buf[80];

if (Qfile(".BMP", "CONVERT", buf, buf, FALSE, "Convert BMP file to WinHelp usable form"))
	{
	LoadFlic(buf);
	SavePic(buf);
	}
}