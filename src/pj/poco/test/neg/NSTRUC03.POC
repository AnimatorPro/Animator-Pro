//See if can use . where should use ->
#pragma poco echo "expect>. where there should be a -> perhaps?"

typedef struct foobar {int foo; char bar;} Foobar;

main()
{
Foobar fb;
Foobar *f = &fb;

f.bar = 'c';
puts("I shouldn't compile");
}
